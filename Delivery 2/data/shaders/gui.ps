varying vec2 v_uv;

uniform sampler2D u_texture;
uniform float u_alpha;

void main() {
    vec4 text_color = texture2D( u_texture, v_uv );

    // Not draw transparent pixels
    if (text_color.a <= 0.5) {
        discard;
    }

	gl_FragColor = vec4( text_color.xyz, text_color.a * u_alpha) ;
}